import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Nick Perry — Software Engineer",
  description: "Portfolio of Nick Perry, Software Engineer and Northwestern CS student. Google SWE intern, builder of Roam, PhishGuard, CineGuide, and more.",
  openGraph: {
    title: "Nick Perry — Software Engineer",
    description: "Software Engineer. Northwestern CS '26. Google intern. Builder.",
    type: "website",
  },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
